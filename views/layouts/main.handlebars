<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Tech Blog</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <style>
    body {
      background-color: #00303F;
    }
    header {
      background-color: #CAE4DB;
      padding: 20px;
      display: flex;
      justify-content: space-between;
    }
    nav {
      display: flex;
      align-items: center;
    }
    nav a {
      color: #00303F;
      margin-right: 10px;
      text-decoration: none;
      font-weight: bold;
    }
    nav a:hover {
      color: #CDAC81;
    }
    .no-button {
      background: none;
      border: none;
      color: #00303F;
      font-weight: bold;
      cursor: pointer;
    }
    .no-button:hover {
      color: #CDAC81;
    }
    .tech-blog {
      color: #00303F;
    }
    h3 {
      font-size: small;
      font: bold;
    }
    .jumbotron {
      background-color: #00303F;
      color: #F5F4F4;
    }
    main.container {
      background-color: #F5F4F4;
      padding: 20px;
      margin-top: 20px;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
    }
    .post {
      background-color: rgb(245, 244, 244);
      border: 1px solid #00303F;
      padding: 10px;
      margin-bottom: 10px;
    }
    .form-control {
      height: 50px; /* set the height to your desired value */
      resize: vertical; /* allow vertical resizing */
    }
    .comments {
  background-color: #F5F4F4;
  padding: 20px;
  margin-top: 20px;
  border-radius: 5px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
}
.comment {
  background-color: #FFFFFF;
  border: 1px solid #00303F;
  padding: 10px;
  margin-bottom: 10px;
}
.comment h5 {
  font-size: small;
  font-weight: bold;
}
.comment p {
  margin-bottom: 0;
}
.comment span {
  font-size: small;
  font-style: italic;
}

    .btn-prime {
      background-color: #CDAC81;
      border-color: #CDAC81;
    }    
    .btn-prime:hover {
      color: rgb(202, 228, 219);
    }

    footer {
      background-color: #CDAC81;
      height: 100px;
    }
  </style>
</head>
<body>
  <div>
    <header>
      <h1>
        <a class="tech-blog" href="/">Tech Blog!</a>
      </h1>
      <nav>
        <!-- Conditionally render login, signup, or logout links -->
        {{#if logged_in}}
        <button class="no-button" id="logout">Logout</button>
        <button onclick="window.location.href='/blog/new'" class="no-button">New Post</button>
        <a href="/dashboard" class="no-button">Dashboard</a>
        {{else}}
        <a href="/login">Login</a>
        <a href="/signup">Signup</a>
        {{/if}}
      </nav>
    </header>
   
    <main class="container">
      <!-- Render the sub layout -->
      {{{ body }}}
      <!-- Render the comments -->
      {{{ comments }}}
    </main>
  </div>

  <!-- Bootstrap JavaScript and jQuery -->
  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
  
  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

  <script src="./js/login.js"></script>
  <script src="./js/signup.js"></script>
</body>
</html>
